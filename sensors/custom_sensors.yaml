platform: template
sensors:
  christmas_season:
    friendly_name: "Christmas Season"
    value_template: >
            {% if now().strftime("%j") | int  > 315 and now().strftime("%j") | int  < 15 %}
              true
            {% else %}
              false
            {% endif %}